import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
	palette: {
		primary: {
			main: '#030603',
			contrastText: '#dbf487',
		},
		secondary: {
			main: '#dbf487',
			contrastText: '#1a3d21',
		},
	},
});
