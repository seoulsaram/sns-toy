export type User = {
	// id: string; // 사용자의 고유한 아이디
	username: string; // 사용자 닉네임 (아이디)
	// password: string; // 사용자 비밀번호
	// name: string; // 사용자 이름
	// email: string; // 사용자 이메일
	// url?: string;
	token: string;
};
